// Database stub — no DB is used by this widget host.
// Replace with a real adapter (e.g. drizzle-orm/node-postgres) if needed.
export function getDb(): never {
  throw new Error("No database configured for this deployment.");
}
